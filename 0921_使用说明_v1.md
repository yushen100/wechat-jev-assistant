# 微信 Jev 免复制助手

## 功能

- 微信位于前台时按 `Ctrl+Alt+J`，读取当前会话标题并开始分析。
- UIA 优先识别当前会话标题，失败时才用本地 RapidOCR 识别标题；聊天正文只从只读数据库读取。
- 不滚动微信聊天记录，不改变当前会话位置。
- 分析期间悬浮窗持续置顶显示，并实时更新读取进度，不会先消失再弹出。
- 主窗口可直接切换 100、150、200、250 条四个档位；数据库分批补读文字和动画表情，不足时使用该会话的加密记忆补齐已有上下文。
- 手机号、邮箱、身份证号、银行卡号和 URL 参数在本地脱敏后才发送给 TypeSafe。
- Jev 返回字面/潜台词、主要意图、紧张程度和建议动作。
- Jev 会围绕最新一条对方消息显示情境卡片：当前阶段、核心需要，以及与场景最相关的三个是/否概率判断。
- 群聊会识别气泡上方昵称，并分别给出每位本轮发言者的意图概率。
- 不同人物使用不同颜色显示；同一人物在消息、重点分析和人物意图中保持同色。
- 没有文字的图片和表情包会按消息块左右位置标记发送方；群聊中会优先关联图片上方识别到的成员昵称。
- 图片或表情内部识别到的短文字不会作为群昵称；只要识别到明确成员昵称，就会为该成员单独判断意图。
- 每条消息记录身份来源、内容来源和视觉指纹；结果窗口显示身份可靠度与内容可靠度。
- 群成员身份不可靠时统一标记为“未知群成员”，不会把图片内文字猜成昵称。
- 数据库原文与分析结果使用 AES-GCM 加密，密钥由当前 Windows 用户的 DPAPI 保护。
- 数据库会话 ID 和消息 ID 会进入同一套加密记忆库；再次分析同一会话时按消息 ID 去重，只补充新消息。
- 人物名称优先使用备注昵称；备注昵称与群昵称不同时显示为“备注昵称（群昵称：…）”。
- 数据库桥接器只读运行，不操作输入框、不监听新消息、不下载媒体、不自动发送消息。

## 安装

1. 右键 `setup.ps1`，选择“使用 PowerShell 运行”。
2. 安装完成后双击 `0921_启动微信Jev助手_v1.cmd`。
3. 打开微信目标聊天，按 `Ctrl+Alt+J`。

本机必须已经存在用户环境变量 `TYPESAFE_API_KEY`。程序只从环境变量读取，不会把 API key 写入项目或日志。

## 首次使用

默认截取区域针对 Windows 微信 4.1 的常见布局。如果 OCR 结果不完整：

1. 打开助手窗口。
2. 点击“区域校准”。
3. 微信窗口会变暗；按住鼠标拖框，选中中间的聊天消息区域。
4. 选中区域保持原亮度，松开鼠标立即保存；按 `Esc` 可以取消。
5. 回到微信按 `Ctrl+Alt+J` 验证。

校准值会按当前显示器分辨率和 DPI 缩放分别保存。拖框截图只存在于内存，不会写入磁盘。

群聊校准时，框选区域必须包含消息气泡上方的成员昵称，否则只能显示为“对方”。

动画表情按数据库消息类型纳入分析；图片、视频、语音和文件不提交。群成员无法从目标群成员表确认时标记为“未知群成员”，不会猜测具体成员。

助手按所选档位和稳定会话 ID 从本地只读数据库分批取回文字与动画表情，不滚动聊天窗口。若该会话有效消息不足所选数量，实际数量会更少；内容过长时自动保留最新部分，并在界面明确显示实际提交数量。

## 历史和隐私

- `data/history.db` 只保存加密载荷，联系人索引使用 SHA-256。
- `data/history.key.dpapi` 只能由当前 Windows 用户解密。
- 会话记忆同样保存在加密历史数据库中，不保存截图。
- “查看历史”支持查看详情、删除选中和清空全部；删除前会二次确认。
- `logs/app.log` 只记录事件类型、消息数量、OCR 置信度和耗时，不记录聊天正文或 API key。

## 常见问题

- **提示请先打开微信**：确保微信聊天窗口是当前活动窗口，不要停留在助手窗口。
- **会话标题识别失败**：保持微信目标会话位于前台，并确认标题完整可见。
- **快捷键无反应**：`Ctrl+Alt+J` 可能被其他软件占用，先关闭冲突软件。
- **API key 无效**：重新配置 `TYPESAFE_API_KEY`，然后彻底重启终端或重新登录 Windows。
- **第一次分析较慢**：数据库只读桥接器首次定位并解密数据库需要额外时间。

## 安全边界

Jev 输出是辅助概率，不代表对方真实想法。低置信度会显示“无法可靠判断”，请以真实沟通为准。
