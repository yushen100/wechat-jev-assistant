# 微信数据库只读隔离验证

- 上游固定提交：`424ffdeeaa9e695191007614703ce78b96e7e6a9e`
- 只加载 `wechatauto/db.py`，不执行上游包入口。
- 不加载发送、监听、UIA 热激活、媒体下载和图片密钥模块。
- 仅使用 `ReadProcessMemory` 扫描当前登录微信的数据库密钥。
- 禁止上游写出明文 `keys.json`。
- 解密数据库只存在于项目 `runtime/probe-*` 临时目录，探测完成后清理。
- `--probe` 只输出数据库、会话和抽样消息数量，不输出正文或联系人。

当前阶段尚未接入主程序。
